<%= @order.id %>
<%= @order.updated_at %>
<%= @order.status %>
<%= @order.name %>
<%= @order.address %>

<% @order.order_items.each do |order_item| %>
  <p><%= order_item.product.name %> <%= order_item.product.price %><%= order_item.quantity %></p>
<% end %>

<%= link_to "Cancel Order", cancel_order_path(@order), method: :post, class: "btn btn-primary" %>